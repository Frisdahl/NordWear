version: "3.8"

services:
  db:
    image: mysql:8
    restart: always
    environment:
      MYSQL_DATABASE: nordwear_db
      MYSQL_USER: user
      MYSQL_PASSWORD: password
      MYSQL_ROOT_PASSWORD: rootpassword
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin" ,"ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

  redis:
    image: redis:alpine
    restart: always
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  server:
    build:
      context: ../
      dockerfile: apps/server/Dockerfile
    restart: always
    environment:
      DATABASE_URL: mysql://user:password@db:3306/nordwear_db
      REDIS_URL: redis://redis:6379
      PORT: 5000
      NODE_ENV: production
      CLIENT_URL: http://localhost
    ports:
      - "5000:5000"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  client:
    build:
      context: ../
      dockerfile: docker/client.Dockerfile
      args:
        - VITE_API_URL=http://localhost:5000/api
    restart: always
    ports:
      - "80:80"
    depends_on:
      - server

volumes:
  db_data: